<Window x:Class="PolyPrint.View.Windows.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PolyPrint.View.Windows"
        mc:Ignorable="d"
        Title="Авторизация" Height="320" Width="450"
        WindowStartupLocation="CenterScreen">

    <Grid>
        <Border Margin="40"
                Padding="24"
                Background="{StaticResource SurfaceBrush}"
                CornerRadius="12">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="150"/>
                    <ColumnDefinition Width="200"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Text="Логин" VerticalAlignment="Center" FontSize="16" Margin="0,0,12,0"/>
                <TextBox x:Name="LoginTextBox" Grid.Row="0" Grid.Column="1" VerticalAlignment="Center"/>

                <TextBlock Grid.Row="1" Grid.Column="0" Text="Пароль" VerticalAlignment="Center" FontSize="16" Margin="0,12,12,0"/>
                <PasswordBox x:Name="PasswordBox" Grid.Row="1" Grid.Column="1" VerticalAlignment="Center" Margin="0,12,0,0"/>

                <StackPanel Grid.Row="2" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,24,0,0">
                    <Button x:Name="LoginButton"
                            Content="Войти"
                            Width="120"
                            Click="LoginButton_Click"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
