<Window x:Class="PolyPrint.View.Windows.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PolyPrint.View.Windows"
        mc:Ignorable="d"
        Title="Авторизация"
        Height="420"
        Width="520"
        MinWidth="480"
        MinHeight="380"
        WindowStartupLocation="CenterScreen">
    <Grid>
        <Border Style="{StaticResource CardStyle}" Width="420" HorizontalAlignment="Center" VerticalAlignment="Center">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0">
                    <TextBlock Text="Добро пожаловать в PolyPrint"
                               Style="{StaticResource SectionTitleTextBlockStyle}"/>
                    <TextBlock Text="Введите учетные данные сотрудника, чтобы продолжить работу в системе."/>
                </StackPanel>

                <StackPanel Grid.Row="1" Margin="0,24,0,0">
                    <StackPanel Margin="0,0,0,16">
                        <TextBlock Text="Логин" FontWeight="SemiBold"/>
                        <TextBox x:Name="LoginTextBox"
                                 MinWidth="200"
                                 HorizontalAlignment="Stretch"/>
                    </StackPanel>
                    <StackPanel>
                        <TextBlock Text="Пароль" FontWeight="SemiBold"/>
                        <PasswordBox x:Name="PasswordBox"
                                     MinWidth="200"
                                     HorizontalAlignment="Stretch"/>
                    </StackPanel>
                </StackPanel>

                <StackPanel Grid.Row="2"
                            Orientation="Horizontal"
                            HorizontalAlignment="Right"
                            Margin="0,28,0,0">
                    <Button Content="Закрыть"
                            Style="{StaticResource SecondaryButtonStyle}"
                            Width="120"
                            Click="CloseButton_Click"/>
                    <Button x:Name="LoginButton"
                            Content="Войти"
                            Style="{StaticResource PrimaryButtonStyle}"
                            Width="140"
                            Margin="12,0,0,0"
                            Click="LoginButton_Click"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
